uri "http://com.ge.research.darpa.aske.ta1.explore/Mach" alias Mach
    (note "This ontology was created by extraction from code by the ANSWER JavaModelExtractorJP.").

import "http://sadl.org/CodeModel.sadl" as codemdl.

 
// Individuals:
Dn is a codemdl:Class,
    has codemdl:containedIn In.
In is a codemdl:Class,
    has codemdl:containedIn Mach.
In2 is a codemdl:Class,
    has codemdl:containedIn Titl.
Mach is a codemdl:Class,
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 47,
    with codemdl:beginsAt 1,
    with codemdl:commentContent "  
                      Mach and Speed of Sound Calculator
            Interactive Program to solve Standard Atmosphere Equations
                     for mach and speed of sound
                Calorically imperfect gas modeled for hypersonics

                          A Java Applet

                     Version 1.2b   - 5 Mar 12

                            Written by 

                             Tom Benson
                            Chuck Trefny
                            Charles Lamp
                       NASA Glenn Research Center

>                              NOTICE
>This software is in the Public Domain.  It may be freely copied and used in
>non-commercial products, assuming proper credit to the author is given.  IT
>MAY NOT BE RESOLD.  If you want to use the software for commercial
>products, contact the author.
>No copyright is claimed in the United States under Title 17, U. S. Code.
>This software is provided \"as is\" without any warranty of any kind, either
>express, implied, or statutory, including, but not limited to, any warranty
>that the software will conform to specifications, any implied warranties of
>merchantability, fitness for a particular purpose, and freedom from
>infringement, and any warranty that the documentation will conform to the
>program, or any warranty that the software will be error free.
>In no event shall NASA be liable for any damages, including, but not
>limited to direct, indirect, special or consequential damages, arising out
>of, resulting from, or in any way connected with this software, whether or
>not based on warranty, contract, tort or otherwise, whether or not injury
>was sustained by persons or property or otherwise, and whether or not loss
>was sustained from, or arose out of the results of, or use of, the software
>or services provided hereunder.

      New Test :
                * move the choice for units
                * add compute button
                * remove Mars model
                * add calorically imperfect gas
                  correct error at low Mach number

                                                     TJB 5 Mar 12

").
Mach.A is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 270,
    with codemdl:beginsAt 270,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 245,
    with codemdl:beginsAt 245,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 284,
    with codemdl:beginsAt 284,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 254,
    with codemdl:beginsAt 254,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 284,
    with codemdl:beginsAt 284,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:setterArgument true,
    has codemdl:varName "A",
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 294,
    with codemdl:beginsAt 294,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 89,
    with codemdl:beginsAt 89,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 269,
    with codemdl:beginsAt 269,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 269,
    with codemdl:beginsAt 269,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 246,
    with codemdl:beginsAt 246,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 268,
    with codemdl:beginsAt 268,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 250,
    with codemdl:beginsAt 250,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 56,
    with codemdl:beginsAt 56,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 281,
    with codemdl:beginsAt 281,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 283,
    with codemdl:beginsAt 283,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 304,
    with codemdl:beginsAt 304,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 246,
    with codemdl:beginsAt 246,
    with codemdl:codeBlock Mach.computeMach).
Mach.CAL_GAM is a codemdl:Method,
    has codemdl:returnTypes ["double"],
    has codemdl:arguments [Mach.CAL_GAM.T,Mach.CAL_GAM.G,Mach.CAL_GAM.Q],
    has codemdl:containedIn Mach.
Mach.CAL_GAM.G is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 109,
    with codemdl:beginsAt 109,
    with codemdl:codeBlock Mach.CAL_GAM),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 109,
    with codemdl:beginsAt 109,
    with codemdl:codeBlock Mach.CAL_GAM),
    has codemdl:varName "G",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 108,
    with codemdl:beginsAt 108,
    with codemdl:codeBlock Mach.CAL_GAM).
Mach.CAL_GAM.Q is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 110,
    with codemdl:beginsAt 110,
    with codemdl:codeBlock Mach.CAL_GAM),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 110,
    with codemdl:beginsAt 110,
    with codemdl:codeBlock Mach.CAL_GAM),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 109,
    with codemdl:beginsAt 109,
    with codemdl:codeBlock Mach.CAL_GAM),
    has codemdl:varName "Q",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 108,
    with codemdl:beginsAt 108,
    with codemdl:codeBlock Mach.CAL_GAM).
Mach.CAL_GAM.T is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 110,
    with codemdl:beginsAt 110,
    with codemdl:codeBlock Mach.CAL_GAM),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 110,
    with codemdl:beginsAt 110,
    with codemdl:codeBlock Mach.CAL_GAM),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 109,
    with codemdl:beginsAt 109,
    with codemdl:codeBlock Mach.CAL_GAM),
    has codemdl:varName "T",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 108,
    with codemdl:beginsAt 108,
    with codemdl:codeBlock Mach.CAL_GAM).
Mach.CAL_SOS is a codemdl:Method,
    has codemdl:returnTypes ["double"],
    has codemdl:arguments [Mach.CAL_SOS.T,Mach.CAL_SOS.G,Mach.CAL_SOS.R,Mach.CAL_SOS.Q],
    has codemdl:containedIn Mach.
Mach.CAL_SOS.G is a codemdl:MethodArgument,
    has codemdl:varName "G",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 98,
    with codemdl:beginsAt 98,
    with codemdl:codeBlock Mach.CAL_SOS).
Mach.CAL_SOS.Q is a codemdl:MethodArgument,
    has codemdl:varName "Q",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 98,
    with codemdl:beginsAt 98,
    with codemdl:codeBlock Mach.CAL_SOS).
Mach.CAL_SOS.R is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 101,
    with codemdl:beginsAt 101,
    with codemdl:codeBlock Mach.CAL_SOS),
    has codemdl:varName "R",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 98,
    with codemdl:beginsAt 98,
    with codemdl:codeBlock Mach.CAL_SOS).
Mach.CAL_SOS.T is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 101,
    with codemdl:beginsAt 101,
    with codemdl:codeBlock Mach.CAL_SOS),
    has codemdl:varName "T",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 98,
    with codemdl:beginsAt 98,
    with codemdl:codeBlock Mach.CAL_SOS).
Mach.CAL_SOS.WOW is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 101,
    with codemdl:beginsAt 101,
    with codemdl:codeBlock Mach.CAL_SOS),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 100,
    with codemdl:beginsAt 99,
    with codemdl:codeBlock Mach.CAL_SOS),
    has codemdl:varName "WOW",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 100,
    with codemdl:beginsAt 99,
    with codemdl:codeBlock Mach.CAL_SOS).
Mach.CAL_T is a codemdl:Method,
    has codemdl:returnTypes ["double"],
    has codemdl:arguments [Mach.CAL_T.TT,Mach.CAL_T.M,Mach.CAL_T.G,Mach.CAL_T.Q],
    has codemdl:containedIn Mach.
Mach.CAL_T.EPS is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 151,
    with codemdl:beginsAt 151,
    with codemdl:codeBlock Mach.CAL_T),
    has codemdl:varName "EPS",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 151,
    with codemdl:beginsAt 151,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_T.G is a codemdl:MethodArgument,
    has codemdl:varName "G",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 148,
    with codemdl:beginsAt 148,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_T.M is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 179,
    with codemdl:beginsAt 179,
    with codemdl:codeBlock Mach.CAL_T),
    has codemdl:varName "M",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 148,
    with codemdl:beginsAt 148,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_T.Q is a codemdl:MethodArgument,
    has codemdl:varName "Q",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 148,
    with codemdl:beginsAt 148,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_T.T is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 181,
    with codemdl:beginsAt 181,
    with codemdl:codeBlock Mach.CAL_T),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 179,
    with codemdl:beginsAt 179,
    with codemdl:codeBlock Mach.CAL_T),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 150,
    with codemdl:beginsAt 150,
    with codemdl:codeBlock Mach.CAL_T),
    has codemdl:varName "T",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 150,
    with codemdl:beginsAt 150,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_T.T1 is a codemdl:MethodVariable,
    has codemdl:varName "T1",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 155,
    with codemdl:beginsAt 155,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_T.T2 is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 154,
    with codemdl:beginsAt 154,
    with codemdl:codeBlock Mach.CAL_T),
    has codemdl:varName "T2",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 154,
    with codemdl:beginsAt 154,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_T.TT is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 179,
    with codemdl:beginsAt 179,
    with codemdl:codeBlock Mach.CAL_T),
    has codemdl:varName "TT",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 148,
    with codemdl:beginsAt 148,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_T.Z is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 152,
    with codemdl:beginsAt 152,
    with codemdl:codeBlock Mach.CAL_T),
    has codemdl:varName "Z",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 152,
    with codemdl:beginsAt 152,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_T.Z1 is a codemdl:MethodVariable,
    has codemdl:varName "Z1",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 156,
    with codemdl:beginsAt 156,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_T.Z2 is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 153,
    with codemdl:beginsAt 153,
    with codemdl:codeBlock Mach.CAL_T),
    has codemdl:varName "Z2",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 153,
    with codemdl:beginsAt 153,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_T.i is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 157,
    with codemdl:beginsAt 157,
    with codemdl:codeBlock Mach.CAL_T),
    has codemdl:varName "i",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 157,
    with codemdl:beginsAt 157,
    with codemdl:codeBlock Mach.CAL_T).
Mach.CAL_TT is a codemdl:Method,
    has codemdl:returnTypes ["double"],
    has codemdl:arguments [Mach.CAL_TT.T,Mach.CAL_TT.M,Mach.CAL_TT.G,Mach.CAL_TT.Q],
    has codemdl:containedIn Mach.
Mach.CAL_TT.EPS is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 115,
    with codemdl:beginsAt 115,
    with codemdl:codeBlock Mach.CAL_TT),
    has codemdl:varName "EPS",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 115,
    with codemdl:beginsAt 115,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TT.G is a codemdl:MethodArgument,
    has codemdl:varName "G",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 113,
    with codemdl:beginsAt 113,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TT.M is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 143,
    with codemdl:beginsAt 143,
    with codemdl:codeBlock Mach.CAL_TT),
    has codemdl:varName "M",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 113,
    with codemdl:beginsAt 113,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TT.Q is a codemdl:MethodArgument,
    has codemdl:varName "Q",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 113,
    with codemdl:beginsAt 113,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TT.T is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 143,
    with codemdl:beginsAt 143,
    with codemdl:codeBlock Mach.CAL_TT),
    has codemdl:varName "T",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 113,
    with codemdl:beginsAt 113,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TT.TT is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 145,
    with codemdl:beginsAt 145,
    with codemdl:codeBlock Mach.CAL_TT),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 143,
    with codemdl:beginsAt 143,
    with codemdl:codeBlock Mach.CAL_TT),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 114,
    with codemdl:beginsAt 114,
    with codemdl:codeBlock Mach.CAL_TT),
    has codemdl:varName "TT",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 114,
    with codemdl:beginsAt 114,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TT.TT1 is a codemdl:MethodVariable,
    has codemdl:varName "TT1",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 119,
    with codemdl:beginsAt 119,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TT.TT2 is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 118,
    with codemdl:beginsAt 118,
    with codemdl:codeBlock Mach.CAL_TT),
    has codemdl:varName "TT2",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 118,
    with codemdl:beginsAt 118,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TT.Z is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 116,
    with codemdl:beginsAt 116,
    with codemdl:codeBlock Mach.CAL_TT),
    has codemdl:varName "Z",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 116,
    with codemdl:beginsAt 116,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TT.Z1 is a codemdl:MethodVariable,
    has codemdl:varName "Z1",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 120,
    with codemdl:beginsAt 120,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TT.Z2 is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 117,
    with codemdl:beginsAt 117,
    with codemdl:codeBlock Mach.CAL_TT),
    has codemdl:varName "Z2",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 117,
    with codemdl:beginsAt 117,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TT.i is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 121,
    with codemdl:beginsAt 121,
    with codemdl:codeBlock Mach.CAL_TT),
    has codemdl:varName "i",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 121,
    with codemdl:beginsAt 121,
    with codemdl:codeBlock Mach.CAL_TT).
Mach.CAL_TV is a codemdl:Method,
    has codemdl:returnTypes ["double"],
    has codemdl:arguments [Mach.CAL_TV.TT,Mach.CAL_TV.V,Mach.CAL_TV.G,Mach.CAL_TV.Q],
    has codemdl:containedIn Mach.
Mach.CAL_TV.EPS is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 187,
    with codemdl:beginsAt 187,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:varName "EPS",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 187,
    with codemdl:beginsAt 187,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.CAL_TV.G is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 215,
    with codemdl:beginsAt 215,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 215,
    with codemdl:beginsAt 215,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:varName "G",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 184,
    with codemdl:beginsAt 184,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.CAL_TV.Q is a codemdl:MethodArgument,
    has codemdl:varName "Q",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 184,
    with codemdl:beginsAt 184,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.CAL_TV.T is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 217,
    with codemdl:beginsAt 217,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 215,
    with codemdl:beginsAt 215,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 186,
    with codemdl:beginsAt 186,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:varName "T",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 186,
    with codemdl:beginsAt 186,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.CAL_TV.T1 is a codemdl:MethodVariable,
    has codemdl:varName "T1",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 191,
    with codemdl:beginsAt 191,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.CAL_TV.T2 is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 190,
    with codemdl:beginsAt 190,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:varName "T2",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 190,
    with codemdl:beginsAt 190,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.CAL_TV.TT is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 215,
    with codemdl:beginsAt 215,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:varName "TT",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 184,
    with codemdl:beginsAt 184,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.CAL_TV.V is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 215,
    with codemdl:beginsAt 215,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 215,
    with codemdl:beginsAt 215,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 215,
    with codemdl:beginsAt 215,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:varName "V",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 184,
    with codemdl:beginsAt 184,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.CAL_TV.Z is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 188,
    with codemdl:beginsAt 188,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:varName "Z",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 188,
    with codemdl:beginsAt 188,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.CAL_TV.Z1 is a codemdl:MethodVariable,
    has codemdl:varName "Z1",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 192,
    with codemdl:beginsAt 192,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.CAL_TV.Z2 is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 189,
    with codemdl:beginsAt 189,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:varName "Z2",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 189,
    with codemdl:beginsAt 189,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.CAL_TV.i is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 193,
    with codemdl:beginsAt 193,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:varName "i",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 193,
    with codemdl:beginsAt 193,
    with codemdl:codeBlock Mach.CAL_TV).
Mach.G is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 224,
    with codemdl:beginsAt 224,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:varName "G",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 56,
    with codemdl:beginsAt 56,
    with codemdl:codeBlock Mach).
Mach.GAM is a codemdl:ClassField,
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 313,
    with codemdl:beginsAt 313,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 282,
    with codemdl:beginsAt 282,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 271,
    with codemdl:beginsAt 271,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 258,
    with codemdl:beginsAt 258,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:varName "GAM",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 56,
    with codemdl:beginsAt 56,
    with codemdl:codeBlock Mach).
Mach.In.Dn.l3 is a codemdl:ClassField,
    has codemdl:varName "l3",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 725,
    with codemdl:beginsAt 725,
    with codemdl:codeBlock Dn).
Mach.In.Dn.l4 is a codemdl:ClassField,
    has codemdl:varName "l4",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 725,
    with codemdl:beginsAt 725,
    with codemdl:codeBlock Dn).
Mach.In.Dn.l5 is a codemdl:ClassField,
    has codemdl:varName "l5",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 725,
    with codemdl:beginsAt 725,
    with codemdl:codeBlock Dn).
Mach.In.Dn.l6 is a codemdl:ClassField,
    has codemdl:varName "l6",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 725,
    with codemdl:beginsAt 725,
    with codemdl:codeBlock Dn).
Mach.In.Dn.l7 is a codemdl:ClassField,
    has codemdl:varName "l7",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 725,
    with codemdl:beginsAt 725,
    with codemdl:codeBlock Dn).
Mach.In.Dn.l8 is a codemdl:ClassField,
    has codemdl:varName "l8",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 725,
    with codemdl:beginsAt 725,
    with codemdl:codeBlock Dn).
Mach.In.Dn.lb is a codemdl:ClassField,
    has codemdl:varName "lb",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 726,
    with codemdl:beginsAt 726,
    with codemdl:codeBlock Dn).
Mach.In.Dn.lc is a codemdl:ClassField,
    has codemdl:varName "lc",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 726,
    with codemdl:beginsAt 726,
    with codemdl:codeBlock Dn).
Mach.In.Dn.ld is a codemdl:ClassField,
    has codemdl:varName "ld",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 726,
    with codemdl:beginsAt 726,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o3 is a codemdl:ClassField,
    has codemdl:varName "o3",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 723,
    with codemdl:beginsAt 723,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o3r is a codemdl:ClassField,
    has codemdl:varName "o3r",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 724,
    with codemdl:beginsAt 724,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o4 is a codemdl:ClassField,
    has codemdl:varName "o4",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 723,
    with codemdl:beginsAt 723,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o4r is a codemdl:ClassField,
    has codemdl:varName "o4r",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 724,
    with codemdl:beginsAt 724,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o5 is a codemdl:ClassField,
    has codemdl:varName "o5",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 723,
    with codemdl:beginsAt 723,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o5r is a codemdl:ClassField,
    has codemdl:varName "o5r",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 724,
    with codemdl:beginsAt 724,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o6 is a codemdl:ClassField,
    has codemdl:varName "o6",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 723,
    with codemdl:beginsAt 723,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o6r is a codemdl:ClassField,
    has codemdl:varName "o6r",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 724,
    with codemdl:beginsAt 724,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o7 is a codemdl:ClassField,
    has codemdl:varName "o7",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 723,
    with codemdl:beginsAt 723,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o7r is a codemdl:ClassField,
    has codemdl:varName "o7r",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 724,
    with codemdl:beginsAt 724,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o8 is a codemdl:ClassField,
    has codemdl:varName "o8",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 723,
    with codemdl:beginsAt 723,
    with codemdl:codeBlock Dn).
Mach.In.Dn.o8r is a codemdl:ClassField,
    has codemdl:varName "o8r",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 724,
    with codemdl:beginsAt 724,
    with codemdl:codeBlock Dn).
Mach.In.Dn.outerparent is a codemdl:ClassField,
    has codemdl:varName "outerparent",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 722,
    with codemdl:beginsAt 722,
    with codemdl:codeBlock Dn).
Mach.In.Titl.In2.handleEvent is a codemdl:Method,
    has codemdl:returnTypes ["boolean"],
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 471,
    with codemdl:beginsAt 471,
    with codemdl:commentContent " Metric units labels"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 453,
    with codemdl:beginsAt 453,
    with codemdl:commentContent " English units labels"),
    has codemdl:arguments [Mach.In.Titl.In2.handleEvent.evt],
    has codemdl:containedIn In2 .
Mach.In.Titl.In2.handleEvent.evt is a codemdl:MethodArgument,
    has codemdl:varName "evt",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 448,
    with codemdl:beginsAt 448,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent).
Mach.In.Titl.In2.lb is a codemdl:ClassField,
    has codemdl:varName "lb",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 420,
    with codemdl:beginsAt 420,
    with codemdl:codeBlock In2).
Mach.In.Titl.In2.lc is a codemdl:ClassField,
    has codemdl:varName "lc",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 420,
    with codemdl:beginsAt 420,
    with codemdl:codeBlock In2).
Mach.In.Titl.In2.plntch is a codemdl:ClassField,
    has codemdl:varName "plntch",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 421,
    with codemdl:beginsAt 421,
    with codemdl:codeBlock In2).
Mach.In.Titl.In2.untch is a codemdl:ClassField,
    has codemdl:varName "untch",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 421,
    with codemdl:beginsAt 421,
    with codemdl:codeBlock In2).
Mach.In.Titl.in2 is a codemdl:ClassField,
    has codemdl:varName "in2",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 404,
    with codemdl:beginsAt 404,
    with codemdl:codeBlock Titl).
Mach.In.Titl.la is a codemdl:ClassField,
    has codemdl:varName "la",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 403,
    with codemdl:beginsAt 403,
    with codemdl:codeBlock Titl).
Mach.In.Up.action is a codemdl:Method,
    has codemdl:returnTypes ["boolean"],
    has codemdl:arguments [Mach.In.Up.action.evt,Mach.In.Up.action.arg],
    has codemdl:containedIn Up.
Mach.In.Up.action.arg is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 564,
    with codemdl:beginsAt 564,
    with codemdl:codeBlock Mach.In.Up.action),
    has codemdl:varName "arg",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 562,
    with codemdl:beginsAt 562,
    with codemdl:codeBlock Mach.In.Up.action).
Mach.In.Up.action.evt is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 569,
    with codemdl:beginsAt 569,
    with codemdl:codeBlock Mach.In.Up.action),
    has codemdl:varName "evt",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 562,
    with codemdl:beginsAt 562,
    with codemdl:codeBlock Mach.In.Up.action).
Mach.In.Up.bt1 is a codemdl:ClassField,
    has codemdl:varName "bt1",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 501,
    with codemdl:beginsAt 501,
    with codemdl:codeBlock Up).
Mach.In.Up.handleBut is a codemdl:Method,
    has codemdl:returnTypes ["void"],
    has codemdl:arguments [Mach.In.Up.handleBut.evt],
    has codemdl:containedIn Up.
Mach.In.Up.handleBut.V1 is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 618,
    with codemdl:beginsAt 618,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:varName "V1",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 615,
    with codemdl:beginsAt 615,
    with codemdl:codeBlock Mach.In.Up.handleBut).
Mach.In.Up.handleBut.V2 is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 620,
    with codemdl:beginsAt 620,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:varName "V2",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 615,
    with codemdl:beginsAt 615,
    with codemdl:codeBlock Mach.In.Up.handleBut).
Mach.In.Up.handleBut.evt is a codemdl:MethodArgument,
    has codemdl:varName "evt",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 614,
    with codemdl:beginsAt 614,
    with codemdl:codeBlock Mach.In.Up.handleBut).
Mach.In.Up.handleBut.v1 is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 652,
    with codemdl:beginsAt 652,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 658,
    with codemdl:beginsAt 658,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 664,
    with codemdl:beginsAt 664,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:varName "v1",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 697,
    with codemdl:beginsAt 697,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 693,
    with codemdl:beginsAt 693,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 708,
    with codemdl:beginsAt 708,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 704,
    with codemdl:beginsAt 704,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 659,
    with codemdl:beginsAt 659,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 710,
    with codemdl:beginsAt 710,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 653,
    with codemdl:beginsAt 653,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 709,
    with codemdl:beginsAt 709,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 655,
    with codemdl:beginsAt 655,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 648,
    with codemdl:beginsAt 648,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 662,
    with codemdl:beginsAt 662,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 698,
    with codemdl:beginsAt 698,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 694,
    with codemdl:beginsAt 694,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 705,
    with codemdl:beginsAt 705,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 660,
    with codemdl:beginsAt 660,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 666,
    with codemdl:beginsAt 666,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 651,
    with codemdl:beginsAt 651,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 712,
    with codemdl:beginsAt 712,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 647,
    with codemdl:beginsAt 647,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 616,
    with codemdl:beginsAt 616,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 649,
    with codemdl:beginsAt 649,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 663,
    with codemdl:beginsAt 663,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 699,
    with codemdl:beginsAt 699,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 695,
    with codemdl:beginsAt 695,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 701,
    with codemdl:beginsAt 701,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 619,
    with codemdl:beginsAt 619,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 706,
    with codemdl:beginsAt 706,
    with codemdl:codeBlock Mach.In.Up.handleBut).
Mach.In.Up.handleBut.v2 is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 640,
    with codemdl:beginsAt 640,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 682,
    with codemdl:beginsAt 682,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 626,
    with codemdl:beginsAt 626,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:varName "v2",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 676,
    with codemdl:beginsAt 676,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 672,
    with codemdl:beginsAt 672,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 630,
    with codemdl:beginsAt 630,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 686,
    with codemdl:beginsAt 686,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 641,
    with codemdl:beginsAt 641,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 636,
    with codemdl:beginsAt 636,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 683,
    with codemdl:beginsAt 683,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 679,
    with codemdl:beginsAt 679,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 637,
    with codemdl:beginsAt 637,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 633,
    with codemdl:beginsAt 633,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 631,
    with codemdl:beginsAt 631,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 677,
    with codemdl:beginsAt 677,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 673,
    with codemdl:beginsAt 673,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 627,
    with codemdl:beginsAt 627,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 687,
    with codemdl:beginsAt 687,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 644,
    with codemdl:beginsAt 644,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 642,
    with codemdl:beginsAt 642,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 638,
    with codemdl:beginsAt 638,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 684,
    with codemdl:beginsAt 684,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 690,
    with codemdl:beginsAt 690,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 688,
    with codemdl:beginsAt 688,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 625,
    with codemdl:beginsAt 625,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 621,
    with codemdl:beginsAt 621,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 616,
    with codemdl:beginsAt 616,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 675,
    with codemdl:beginsAt 675,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 671,
    with codemdl:beginsAt 671,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 629,
    with codemdl:beginsAt 629,
    with codemdl:codeBlock Mach.In.Up.handleBut).
Mach.In.Up.handleProb is a codemdl:Method,
    has codemdl:returnTypes ["void"],
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 598,
    with codemdl:beginsAt 596,
    with codemdl:commentContent " Metric units labels"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 584,
    with codemdl:beginsAt 582,
    with codemdl:commentContent " English units labels"),
    has codemdl:arguments [Mach.In.Up.handleProb.obj],
    has codemdl:containedIn Up.
Mach.In.Up.handleProb.obj is a codemdl:MethodArgument,
    has codemdl:varName "obj",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 576,
    with codemdl:beginsAt 576,
    with codemdl:codeBlock Mach.In.Up.handleProb).
Mach.In.Up.insets is a codemdl:Method,
    has codemdl:returnTypes ["Insets"],
    has codemdl:containedIn Up.
Mach.In.Up.l1 is a codemdl:ClassField,
    has codemdl:varName "l1",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 500,
    with codemdl:beginsAt 500,
    with codemdl:codeBlock Up).
Mach.In.Up.l1u is a codemdl:ClassField,
    has codemdl:varName "l1u",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 500,
    with codemdl:beginsAt 500,
    with codemdl:codeBlock Up).
Mach.In.Up.l2u is a codemdl:ClassField,
    has codemdl:varName "l2u",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 500,
    with codemdl:beginsAt 500,
    with codemdl:codeBlock Up).
Mach.In.Up.la is a codemdl:ClassField,
    has codemdl:varName "la",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 500,
    with codemdl:beginsAt 500,
    with codemdl:codeBlock Up).
Mach.In.Up.o1 is a codemdl:ClassField,
    has codemdl:varName "o1",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 499,
    with codemdl:beginsAt 499,
    with codemdl:codeBlock Up).
Mach.In.Up.o2 is a codemdl:ClassField,
    has codemdl:varName "o2",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 499,
    with codemdl:beginsAt 499,
    with codemdl:codeBlock Up).
Mach.In.Up.tch is a codemdl:ClassField,
    has codemdl:varName "tch",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 502,
    with codemdl:beginsAt 502,
    with codemdl:codeBlock Up).
Mach.In.Up.vch is a codemdl:ClassField,
    has codemdl:varName "vch",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 502,
    with codemdl:beginsAt 502,
    with codemdl:codeBlock Up).
Mach.In.dn is a codemdl:ClassField,
    has codemdl:varName "dn",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 383,
    with codemdl:beginsAt 383,
    with codemdl:codeBlock In).
Mach.In.insets is a codemdl:Method,
    has codemdl:returnTypes ["Insets"],
    has codemdl:containedIn In.
Mach.In.outerparent is a codemdl:ClassField,
    has codemdl:varName "outerparent",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 380,
    with codemdl:beginsAt 380,
    with codemdl:codeBlock In).
Mach.In.titl is a codemdl:ClassField,
    has codemdl:varName "titl",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 381,
    with codemdl:beginsAt 381,
    with codemdl:codeBlock In).
Mach.In.up is a codemdl:ClassField,
    has codemdl:varName "up",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 382,
    with codemdl:beginsAt 382,
    with codemdl:codeBlock In).
Mach.M is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 312,
    with codemdl:beginsAt 312,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 644,
    with codemdl:beginsAt 644,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 254,
    with codemdl:beginsAt 254,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 250,
    with codemdl:beginsAt 250,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 690,
    with codemdl:beginsAt 690,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 283,
    with codemdl:beginsAt 283,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 56,
    with codemdl:beginsAt 56,
    with codemdl:codeBlock Mach),
    has codemdl:setterArgument true,
    has codemdl:varName "M",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 90,
    with codemdl:beginsAt 90,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 270,
    with codemdl:beginsAt 270,
    with codemdl:codeBlock Mach.computeMach).
Mach.Q is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 93,
    with codemdl:beginsAt 93,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:varName "Q",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 56,
    with codemdl:beginsAt 56,
    with codemdl:codeBlock Mach).
Mach.R is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 215,
    with codemdl:beginsAt 215,
    with codemdl:codeBlock Mach.CAL_TV),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 94,
    with codemdl:beginsAt 94,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:varName "R",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 56,
    with codemdl:beginsAt 56,
    with codemdl:codeBlock Mach).
Mach.T is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 306,
    with codemdl:beginsAt 306,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 234,
    with codemdl:beginsAt 234,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 87,
    with codemdl:beginsAt 87,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 280,
    with codemdl:beginsAt 280,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 56,
    with codemdl:beginsAt 56,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 296,
    with codemdl:beginsAt 296,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 237,
    with codemdl:beginsAt 237,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 228,
    with codemdl:beginsAt 228,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 231,
    with codemdl:beginsAt 231,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:varName "T",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 240,
    with codemdl:beginsAt 240,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 267,
    with codemdl:beginsAt 267,
    with codemdl:codeBlock Mach.computeMach).
Mach.TQTT is a codemdl:Method,
    has codemdl:returnTypes ["double"],
    has codemdl:arguments [Mach.TQTT.M,Mach.TQTT.G],
    has codemdl:containedIn Mach.
Mach.TQTT.G is a codemdl:MethodArgument,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 105,
    with codemdl:beginsAt 105,
    with codemdl:codeBlock Mach.TQTT),
    has codemdl:varName "G",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 104,
    with codemdl:beginsAt 104,
    with codemdl:codeBlock Mach.TQTT).
Mach.TQTT.M is a codemdl:MethodArgument,
    has codemdl:varName "M",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 104,
    with codemdl:beginsAt 104,
    with codemdl:codeBlock Mach.TQTT).
Mach.TT is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 307,
    with codemdl:beginsAt 307,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 91,
    with codemdl:beginsAt 91,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:varName "TT",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 666,
    with codemdl:beginsAt 666,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 712,
    with codemdl:beginsAt 712,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 56,
    with codemdl:beginsAt 56,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 257,
    with codemdl:beginsAt 257,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 297,
    with codemdl:beginsAt 297,
    with codemdl:codeBlock Mach.computeMach).
Mach.V is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 274,
    with codemdl:beginsAt 274,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 270,
    with codemdl:beginsAt 270,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 679,
    with codemdl:beginsAt 679,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 295,
    with codemdl:beginsAt 295,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 86,
    with codemdl:beginsAt 86,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 250,
    with codemdl:beginsAt 250,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 285,
    with codemdl:beginsAt 285,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:varName "V",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 254,
    with codemdl:beginsAt 254,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 633,
    with codemdl:beginsAt 633,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 285,
    with codemdl:beginsAt 285,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 283,
    with codemdl:beginsAt 283,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 56,
    with codemdl:beginsAt 56,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 305,
    with codemdl:beginsAt 305,
    with codemdl:codeBlock Mach.computeMach).
Mach.a0 is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 290,
    with codemdl:beginsAt 290,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 89,
    with codemdl:beginsAt 89,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 88,
    with codemdl:beginsAt 88,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 277,
    with codemdl:beginsAt 277,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 243,
    with codemdl:beginsAt 243,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 244,
    with codemdl:beginsAt 244,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 89,
    with codemdl:beginsAt 89,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 266,
    with codemdl:beginsAt 266,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 249,
    with codemdl:beginsAt 249,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 253,
    with codemdl:beginsAt 253,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 264,
    with codemdl:beginsAt 264,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 300,
    with codemdl:beginsAt 300,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:varName "a0",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 265,
    with codemdl:beginsAt 265,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 265,
    with codemdl:beginsAt 265,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 55,
    with codemdl:beginsAt 55,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 278,
    with codemdl:beginsAt 278,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 276,
    with codemdl:beginsAt 276,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 244,
    with codemdl:beginsAt 244,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 90,
    with codemdl:beginsAt 90,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 278,
    with codemdl:beginsAt 278,
    with codemdl:codeBlock Mach.computeMach).
Mach.alt is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 233,
    with codemdl:beginsAt 233,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 233,
    with codemdl:beginsAt 233,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:varName "alt",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 236,
    with codemdl:beginsAt 236,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 227,
    with codemdl:beginsAt 227,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 655,
    with codemdl:beginsAt 655,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 230,
    with codemdl:beginsAt 230,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 327,
    with codemdl:beginsAt 327,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 701,
    with codemdl:beginsAt 701,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 234,
    with codemdl:beginsAt 234,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 239,
    with codemdl:beginsAt 239,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 230,
    with codemdl:beginsAt 230,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 85,
    with codemdl:beginsAt 85,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 228,
    with codemdl:beginsAt 228,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 236,
    with codemdl:beginsAt 236,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 239,
    with codemdl:beginsAt 239,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 341,
    with codemdl:beginsAt 341,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 240,
    with codemdl:beginsAt 240,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 87,
    with codemdl:beginsAt 87,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 54,
    with codemdl:beginsAt 54,
    with codemdl:codeBlock Mach).
Mach.computeMach is a codemdl:Method,
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 285,
    with codemdl:beginsAt 285,
    with codemdl:commentContent " v in mph"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 243,
    with codemdl:beginsAt 243,
    with codemdl:commentContent " feet /sec"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 284,
    with codemdl:beginsAt 284,
    with codemdl:commentContent " mph"),
    has codemdl:returnTypes ["void"],
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 228,
    with codemdl:beginsAt 228,
    with codemdl:commentContent " Troposphere"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 278,
    with codemdl:beginsAt 278,
    with codemdl:commentContent " mph"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 246,
    with codemdl:beginsAt 246,
    with codemdl:commentContent " mph"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 264,
    with codemdl:beginsAt 264,
    with codemdl:commentContent " feet /sec"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 224,
    with codemdl:beginsAt 224,
    with codemdl:commentContent " ft2/sec2 R "),
    has codemdl:containedIn Mach,
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 265,
    with codemdl:beginsAt 265,
    with codemdl:commentContent " mph"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 276,
    with codemdl:beginsAt 276,
    with codemdl:commentContent " feet /sec"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 274,
    with codemdl:beginsAt 274,
    with codemdl:commentContent " v in ft/sec"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 231,
    with codemdl:beginsAt 231,
    with codemdl:commentContent " Stratosphere"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 244,
    with codemdl:beginsAt 244,
    with codemdl:commentContent " mph"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 251,
    with codemdl:beginsAt 248,
    with codemdl:commentContent " compute either mach or velocity "),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 269,
    with codemdl:beginsAt 269,
    with codemdl:commentContent " mph"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 279,
    with codemdl:beginsAt 279,
    with codemdl:commentContent " v in mph"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 223,
    with codemdl:beginsAt 223,
    with codemdl:commentContent " Earth  standard day").
Mach.filter0 is a codemdl:Method,
    has codemdl:returnTypes ["int"],
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 351,
    with codemdl:beginsAt 351,
    with codemdl:commentContent "  integer output"),
    has codemdl:arguments [Mach.filter0.inumbr],
    has codemdl:containedIn Mach.
Mach.filter0.intermed is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 356,
    with codemdl:beginsAt 356,
    with codemdl:codeBlock Mach.filter0),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 354,
    with codemdl:beginsAt 354,
    with codemdl:codeBlock Mach.filter0),
    has codemdl:varName "intermed",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 352,
    with codemdl:beginsAt 352,
    with codemdl:codeBlock Mach.filter0).
Mach.filter0.inumbr is a codemdl:MethodArgument,
    has codemdl:varName "inumbr",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 349,
    with codemdl:beginsAt 349,
    with codemdl:codeBlock Mach.filter0).
Mach.filter0.number is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 355,
    with codemdl:beginsAt 355,
    with codemdl:codeBlock Mach.filter0),
    has codemdl:varName "number",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 351,
    with codemdl:beginsAt 351,
    with codemdl:codeBlock Mach.filter0).
Mach.filter3 is a codemdl:Method,
    has codemdl:returnTypes ["float"],
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 361,
    with codemdl:beginsAt 361,
    with codemdl:commentContent "  output only to .001"),
    has codemdl:arguments [Mach.filter3.inumbr],
    has codemdl:containedIn Mach.
Mach.filter3.intermed is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 364,
    with codemdl:beginsAt 364,
    with codemdl:codeBlock Mach.filter3),
    has codemdl:varName "intermed",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 362,
    with codemdl:beginsAt 362,
    with codemdl:codeBlock Mach.filter3).
Mach.filter3.inumbr is a codemdl:MethodArgument,
    has codemdl:varName "inumbr",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 359,
    with codemdl:beginsAt 359,
    with codemdl:codeBlock Mach.filter3).
Mach.filter3.number is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 366,
    with codemdl:beginsAt 366,
    with codemdl:codeBlock Mach.filter3),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 365,
    with codemdl:beginsAt 365,
    with codemdl:codeBlock Mach.filter3),
    has codemdl:varName "number",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 361,
    with codemdl:beginsAt 361,
    with codemdl:codeBlock Mach.filter3).
Mach.filter5 is a codemdl:Method,
    has codemdl:returnTypes ["float"],
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 371,
    with codemdl:beginsAt 371,
    with codemdl:commentContent "  output only to .00001"),
    has codemdl:arguments [Mach.filter5.inumbr],
    has codemdl:containedIn Mach.
Mach.filter5.intermed is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 374,
    with codemdl:beginsAt 374,
    with codemdl:codeBlock Mach.filter5),
    has codemdl:varName "intermed",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 372,
    with codemdl:beginsAt 372,
    with codemdl:codeBlock Mach.filter5).
Mach.filter5.inumbr is a codemdl:MethodArgument,
    has codemdl:varName "inumbr",
    has codemdl:reference (a codemdl:Reference
    with codemdl:input true,
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 369,
    with codemdl:beginsAt 369,
    with codemdl:codeBlock Mach.filter5).
Mach.filter5.number is a codemdl:MethodVariable,
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 376,
    with codemdl:beginsAt 376,
    with codemdl:codeBlock Mach.filter5),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 375,
    with codemdl:beginsAt 375,
    with codemdl:codeBlock Mach.filter5),
    has codemdl:varName "number",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 371,
    with codemdl:beginsAt 371,
    with codemdl:codeBlock Mach.filter5).
Mach.gama is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 91,
    with codemdl:beginsAt 91,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 243,
    with codemdl:beginsAt 243,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 54,
    with codemdl:beginsAt 54,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 88,
    with codemdl:beginsAt 88,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 276,
    with codemdl:beginsAt 276,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 275,
    with codemdl:beginsAt 275,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 224,
    with codemdl:beginsAt 224,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 311,
    with codemdl:beginsAt 311,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 256,
    with codemdl:beginsAt 256,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 275,
    with codemdl:beginsAt 275,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:varName "gama",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 263,
    with codemdl:beginsAt 263,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 264,
    with codemdl:beginsAt 264,
    with codemdl:codeBlock Mach.computeMach).
Mach.in is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 70,
    with codemdl:beginsAt 70,
    with codemdl:codeBlock Mach.init),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 68,
    with codemdl:beginsAt 68,
    with codemdl:codeBlock Mach.init),
    has codemdl:varName "in",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 60,
    with codemdl:beginsAt 60,
    with codemdl:codeBlock Mach).
Mach.init is a codemdl:Method,
    has codemdl:returnTypes ["void"],
    has codemdl:containedIn Mach.
Mach.insets is a codemdl:Method,
    has codemdl:returnTypes ["Insets"],
    has codemdl:containedIn Mach.
Mach.loadInpt is a codemdl:Method,
    has codemdl:returnTypes ["void"],
    has codemdl:containedIn Mach.
Mach.lrat is a codemdl:ClassField,
    has codemdl:varName "lrat",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 55,
    with codemdl:beginsAt 55,
    with codemdl:codeBlock Mach).
Mach.lunits is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 595,
    with codemdl:beginsAt 595,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 333,
    with codemdl:beginsAt 333,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 452,
    with codemdl:beginsAt 452,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 57,
    with codemdl:beginsAt 57,
    with codemdl:codeBlock Mach),
    has codemdl:varName "lunits",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 470,
    with codemdl:beginsAt 470,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 669,
    with codemdl:beginsAt 669,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 319,
    with codemdl:beginsAt 319,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 289,
    with codemdl:beginsAt 289,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 299,
    with codemdl:beginsAt 299,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 623,
    with codemdl:beginsAt 623,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 581,
    with codemdl:beginsAt 581,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 82,
    with codemdl:beginsAt 82,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 450,
    with codemdl:beginsAt 450,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent).
Mach.mach is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 310,
    with codemdl:beginsAt 310,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 256,
    with codemdl:beginsAt 256,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 55,
    with codemdl:beginsAt 55,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 256,
    with codemdl:beginsAt 256,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 90,
    with codemdl:beginsAt 90,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 644,
    with codemdl:beginsAt 644,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 91,
    with codemdl:beginsAt 91,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 690,
    with codemdl:beginsAt 690,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 338,
    with codemdl:beginsAt 338,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 253,
    with codemdl:beginsAt 253,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 249,
    with codemdl:beginsAt 249,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 324,
    with codemdl:beginsAt 324,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 263,
    with codemdl:beginsAt 263,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 266,
    with codemdl:beginsAt 266,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 263,
    with codemdl:beginsAt 263,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 91,
    with codemdl:beginsAt 91,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:varName "mach",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 277,
    with codemdl:beginsAt 277,
    with codemdl:codeBlock Mach.computeMach).
Mach.mode is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 716,
    with codemdl:beginsAt 716,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 610,
    with codemdl:beginsAt 610,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 489,
    with codemdl:beginsAt 489,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 315,
    with codemdl:beginsAt 315,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 83,
    with codemdl:beginsAt 83,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:varName "mode",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 58,
    with codemdl:beginsAt 58,
    with codemdl:codeBlock Mach).
Mach.planet is a codemdl:ClassField,
    has codemdl:varName "planet",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 57,
    with codemdl:beginsAt 57,
    with codemdl:codeBlock Mach).
Mach.press is a codemdl:ClassField,
    has codemdl:varName "press",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 54,
    with codemdl:beginsAt 54,
    with codemdl:codeBlock Mach).
Mach.prs is a codemdl:ClassField,
    has codemdl:varName "prs",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 57,
    with codemdl:beginsAt 57,
    with codemdl:codeBlock Mach).
Mach.rgas is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 276,
    with codemdl:beginsAt 276,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 275,
    with codemdl:beginsAt 275,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 264,
    with codemdl:beginsAt 264,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 243,
    with codemdl:beginsAt 243,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 223,
    with codemdl:beginsAt 223,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 88,
    with codemdl:beginsAt 88,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:varName "rgas",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 55,
    with codemdl:beginsAt 55,
    with codemdl:codeBlock Mach).
Mach.rho is a codemdl:ClassField,
    has codemdl:varName "rho",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 55,
    with codemdl:beginsAt 55,
    with codemdl:codeBlock Mach).
Mach.rho0 is a codemdl:ClassField,
    has codemdl:varName "rho0",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 55,
    with codemdl:beginsAt 55,
    with codemdl:codeBlock Mach).
Mach.setDefaults is a codemdl:Method,
    has codemdl:returnTypes ["void"],
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 89,
    with codemdl:beginsAt 89,
    with codemdl:commentContent " mph"),
    has codemdl:comment (a codemdl:Comment
    with codemdl:endsAt 88,
    with codemdl:beginsAt 88,
    with codemdl:commentContent " feet /sec"),
    has codemdl:containedIn Mach.
Mach.temp is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 88,
    with codemdl:beginsAt 88,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:varName "temp",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 256,
    with codemdl:beginsAt 256,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 240,
    with codemdl:beginsAt 240,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 264,
    with codemdl:beginsAt 264,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 243,
    with codemdl:beginsAt 243,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 228,
    with codemdl:beginsAt 228,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 302,
    with codemdl:beginsAt 302,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 91,
    with codemdl:beginsAt 91,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 292,
    with codemdl:beginsAt 292,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 275,
    with codemdl:beginsAt 275,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 54,
    with codemdl:beginsAt 54,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 263,
    with codemdl:beginsAt 263,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 237,
    with codemdl:beginsAt 237,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 87,
    with codemdl:beginsAt 87,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 231,
    with codemdl:beginsAt 231,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 234,
    with codemdl:beginsAt 234,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 276,
    with codemdl:beginsAt 276,
    with codemdl:codeBlock Mach.computeMach).
Mach.ther is a codemdl:ClassField,
    has codemdl:varName "ther",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 57,
    with codemdl:beginsAt 57,
    with codemdl:codeBlock Mach).
Mach.tparam is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 657,
    with codemdl:beginsAt 657,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 81,
    with codemdl:beginsAt 81,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 591,
    with codemdl:beginsAt 591,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 484,
    with codemdl:beginsAt 484,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 646,
    with codemdl:beginsAt 646,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:varName "tparam",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 463,
    with codemdl:beginsAt 463,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 261,
    with codemdl:beginsAt 261,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 692,
    with codemdl:beginsAt 692,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 340,
    with codemdl:beginsAt 340,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 588,
    with codemdl:beginsAt 588,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 466,
    with codemdl:beginsAt 466,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 343,
    with codemdl:beginsAt 343,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 329,
    with codemdl:beginsAt 329,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 57,
    with codemdl:beginsAt 57,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 481,
    with codemdl:beginsAt 481,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 602,
    with codemdl:beginsAt 602,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 326,
    with codemdl:beginsAt 326,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 605,
    with codemdl:beginsAt 605,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 703,
    with codemdl:beginsAt 703,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 226,
    with codemdl:beginsAt 226,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 579,
    with codemdl:beginsAt 579,
    with codemdl:codeBlock Mach.In.Up.handleProb).
Mach.ttot is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 263,
    with codemdl:beginsAt 263,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:varName "ttot",
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 330,
    with codemdl:beginsAt 330,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 303,
    with codemdl:beginsAt 303,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 54,
    with codemdl:beginsAt 54,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 256,
    with codemdl:beginsAt 256,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 293,
    with codemdl:beginsAt 293,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 666,
    with codemdl:beginsAt 666,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 344,
    with codemdl:beginsAt 344,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 275,
    with codemdl:beginsAt 275,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 91,
    with codemdl:beginsAt 91,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 712,
    with codemdl:beginsAt 712,
    with codemdl:codeBlock Mach.In.Up.handleBut).
Mach.vel is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 274,
    with codemdl:beginsAt 274,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 633,
    with codemdl:beginsAt 633,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 277,
    with codemdl:beginsAt 277,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 301,
    with codemdl:beginsAt 301,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 266,
    with codemdl:beginsAt 266,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 90,
    with codemdl:beginsAt 90,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 321,
    with codemdl:beginsAt 321,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 249,
    with codemdl:beginsAt 249,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 679,
    with codemdl:beginsAt 679,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 275,
    with codemdl:beginsAt 275,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 274,
    with codemdl:beginsAt 274,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 54,
    with codemdl:beginsAt 54,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 86,
    with codemdl:beginsAt 86,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 253,
    with codemdl:beginsAt 253,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 335,
    with codemdl:beginsAt 335,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:setterArgument true,
    has codemdl:reference (a codemdl:Reference
    with codemdl:output true,
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 291,
    with codemdl:beginsAt 291,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 275,
    with codemdl:beginsAt 275,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:varName "vel",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 279,
    with codemdl:beginsAt 279,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 279,
    with codemdl:beginsAt 279,
    with codemdl:codeBlock Mach.computeMach).
Mach.vparam is a codemdl:ClassField,
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 80,
    with codemdl:beginsAt 80,
    with codemdl:codeBlock Mach.setDefaults),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 670,
    with codemdl:beginsAt 670,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 624,
    with codemdl:beginsAt 624,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 457,
    with codemdl:beginsAt 457,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 582,
    with codemdl:beginsAt 582,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 599,
    with codemdl:beginsAt 599,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 323,
    with codemdl:beginsAt 323,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 337,
    with codemdl:beginsAt 337,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Reassigned,
    with codemdl:endsAt 578,
    with codemdl:beginsAt 578,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 262,
    with codemdl:beginsAt 262,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 475,
    with codemdl:beginsAt 475,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 585,
    with codemdl:beginsAt 585,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 252,
    with codemdl:beginsAt 252,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 635,
    with codemdl:beginsAt 635,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Defined,
    with codemdl:endsAt 57,
    with codemdl:beginsAt 57,
    with codemdl:codeBlock Mach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 478,
    with codemdl:beginsAt 478,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 273,
    with codemdl:beginsAt 273,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:varName "vparam",
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 681,
    with codemdl:beginsAt 681,
    with codemdl:codeBlock Mach.In.Up.handleBut),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 596,
    with codemdl:beginsAt 596,
    with codemdl:codeBlock Mach.In.Up.handleProb),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 248,
    with codemdl:beginsAt 248,
    with codemdl:codeBlock Mach.computeMach),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 320,
    with codemdl:beginsAt 320,
    with codemdl:codeBlock Mach.loadInpt),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 460,
    with codemdl:beginsAt 460,
    with codemdl:codeBlock Mach.In.Titl.In2.handleEvent),
    has codemdl:reference (a codemdl:Reference
    with codemdl:usage codemdl:Used,
    with codemdl:endsAt 334,
    with codemdl:beginsAt 334,
    with codemdl:codeBlock Mach.loadInpt).
Titl is a codemdl:Class,
    has codemdl:containedIn In.
Up is a codemdl:Class,
    has codemdl:containedIn In.
